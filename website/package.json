{
  "name": "afraid-asteroid",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/generate-snapshot.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/generate-snapshot.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.7",
    "astro": "^6.0.4",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@prodshape/cli": "^0.16.0"
  }
}
